<h4>{{title}}</h4>
<p>{{{excerpt}}}</p>
<a href="#">{{{view 'icons/ic_arrow-right'}}} See more</a>
{{!-- 


<a href="#{{categories.0.slug}}/{{slug}}" class="publication__thumbnail" {{#if inCache}}style="background-image: url({{thumbnail_images.post-thumbnail.url}})"{{/if}} alt="{{{title}}}"></a>

<section class="publication__content">
	<section class="publication__content__details">
		<time datetime="{{date}}" class="publication__content__details__date">{{date}}</time>
	</section>

	<a href="#{{categories.0.slug}}/{{slug}}" class="publication__content__title">{{{title}}}</a>

	<a href="#{{categories.0.slug}}/{{slug}}" class="publication__content__excerpt">{{{excerpt}}}</a>

	<section class="publication__content__details bottom">
		<a href="#" class="publication__content__details__author">{{author.nickname}}</a>
	</section>
</section>

<a href="#{{categories.0.slug}}" class="publication__category">{{categories.0.title}}</a> --}}